pipeline {
    agent any
    stages {
        stage('Clone Code') {
            steps {
                git branch: 'master', url: 'https://github.com/Joykinuthia/gallery.git'
            }
        }
         stage('Install Dependencies') {
            steps {
                sh 'npm install' 
            }
        }